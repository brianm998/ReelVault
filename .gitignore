# Rust
target/
Cargo.lock
**/*.rs.bk

# Kotlin / Android
.gradle/
build/
.idea/
*.iml
*.apk
*.aar
*.aab
local.properties

# macOS
.DS_Store
.swiftpm/
.build/
.build2/
.buildtmp/
*.xcodeproj/xcuserdata/
*.playground
# iOS Xcode project is generated by XcodeGen (ios/project.yml); don't commit it.
ios/*.xcodeproj/
ios/build/
ios/DerivedData/
ios/DerivedData*/
# Embedded-core XCFramework is a build artifact (ios/build-core-xcframework.sh).
ios/Frameworks/
# Embedded-core native libs are build artifacts (android/build-core.sh).
android/src/main/jniLibs/

# IDE
.vscode/
*.swp
*.swo
*~
.env
.env.local

# Release staging directories (build outputs, not source)
/dist/
desktop/release-bin/

# Database & Cache
*.db
*.sqlite
*.sqlite3
.cache/
.thumbnails/

# OS
Thumbs.db

# Python bytecode (tools/ helper scripts)
__pycache__/
*.py[cod]

# Claude Code — personal/local settings (shared config lives in .claude/settings.json)
.claude/settings.local.json

# Local scratch notes and draft docs (not source)
/prompt
/prompts
/releases/v0.1.0.md
